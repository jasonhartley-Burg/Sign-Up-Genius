# Sign-Up-Genius Volunteer Dashboard v0.5.1

Maintenance release for v0.5.0. It hardens all browser-side date formatting so malformed or missing timestamps cannot produce an `Invalid time value` page failure.

Public scoreboard: `/`
Admin console: `/admin`

The admin console uses `ADMIN_TOKEN` (preferred) and retains the v0.5.0 management features.
