import type{Env}from"./types";
export async function getSettings(env:Env){const rows=await env.DB.prepare("SELECT key,value FROM settings").all<any>();const m=new Map((rows.results||[]).map((x:any)=>[x.key,x.value]));return{estimateUntimedEnabled:m.get("estimate_untimed_enabled")==="1",estimateUntimedHours:Number(m.get("estimate_untimed_hours")||6)}}
export async function dashboard(env:Env){const cfg=await getSettings(env);const est=cfg.estimateUntimedEnabled?cfg.estimateUntimedHours:0;const s=await env.DB.prepare(`SELECT COALESCE(SUM(quantity),0) totalAssignments,COALESCE(SUM(CASE WHEN status='open' THEN quantity ELSE 0 END),0) openSlots,COALESCE(SUM(CASE WHEN status='filled' THEN quantity ELSE 0 END),0) assignedSlots,COALESCE(SUM(CASE WHEN hours_known=0 THEN quantity ELSE 0 END),0) tbdAssignments,COALESCE(SUM(CASE WHEN hours_known=1 THEN hours*quantity ELSE 0 END),0) knownNeeded,COALESCE(SUM(CASE WHEN status='filled' AND hours_known=1 THEN hours*quantity ELSE 0 END),0) knownFilled,COALESCE(SUM(CASE WHEN hours_known=0 THEN quantity ELSE 0 END),0) tbdNeededQty,COALESCE(SUM(CASE WHEN status='filled' AND hours_known=0 THEN quantity ELSE 0 END),0) tbdFilledQty FROM volunteer_slots`).first<any>();const knownNeeded=Number(s?.knownNeeded||0),knownFilled=Number(s?.knownFilled||0),needed=knownNeeded+Number(s?.tbdNeededQty||0)*est,filled=knownFilled+Number(s?.tbdFilledQty||0)*est;const e=await env.DB.prepare(`SELECT e.id,e.signupgenius_id signupgeniusId,e.title,e.event_date eventDate,e.location,COALESCE(SUM(CASE WHEN v.hours_known=1 THEN v.hours*v.quantity ELSE 0 END),0) knownNeeded,COALESCE(SUM(CASE WHEN v.status='filled' AND v.hours_known=1 THEN v.hours*v.quantity ELSE 0 END),0) knownFilled,COALESCE(SUM(CASE WHEN v.hours_known=0 THEN v.quantity ELSE 0 END),0) tbdQty,COALESCE(SUM(CASE WHEN v.status='filled' AND v.hours_known=0 THEN v.quantity ELSE 0 END),0) tbdFilledQty,COALESCE(SUM(CASE WHEN v.status='open' THEN v.quantity ELSE 0 END),0) openSlots,COALESCE(SUM(CASE WHEN v.status='filled' THEN v.quantity ELSE 0 END),0) assignedSlots FROM events e LEFT JOIN volunteer_slots v ON v.event_id=e.id GROUP BY e.id ORDER BY e.event_date`).all<any>();const p=await env.DB.prepare("SELECT id,name,required_hours requiredHours FROM programs ORDER BY name").all();const l=await env.DB.prepare("SELECT sync_time syncTime,status FROM sync_log ORDER BY id DESC LIMIT 1").first<any>();const volunteers=await env.DB.prepare(`SELECT COALESCE(NULLIF(volunteer_name,''),volunteer_email,'Unknown') name,volunteer_email email,COALESCE(SUM(CASE WHEN hours_known=1 THEN hours*quantity ELSE 0 END),0) knownHours,COALESCE(SUM(CASE WHEN hours_known=0 THEN quantity ELSE 0 END),0) tbdAssignments,COALESCE(SUM(quantity),0) assignments FROM volunteer_slots WHERE status='filled' GROUP BY LOWER(COALESCE(volunteer_email,volunteer_name)) ORDER BY knownHours DESC,name LIMIT 100`).all<any>();return{settings:cfg,summary:{hoursNeeded:needed,hoursFilled:filled,hoursRemaining:Math.max(0,needed-filled),knownHoursNeeded:knownNeeded,knownHoursFilled:knownFilled,estimatedHoursNeeded:Number(s?.tbdNeededQty||0)*est,estimatedHoursFilled:Number(s?.tbdFilledQty||0)*est,fillRate:needed?filled/needed*100:0,activeEvents:e.results.length,totalAssignments:Number(s?.totalAssignments||0),openSlots:Number(s?.openSlots||0),assignedSlots:Number(s?.assignedSlots||0),tbdAssignments:Number(s?.tbdAssignments||0),lastSyncAt:l?.syncTime||null,lastSyncStatus:l?.status||null},programs:p.results.map((x:any)=>({...x,needed:0,filled:0,remaining:0,fillRate:0})),events:e.results.map((x:any)=>({...x,hoursNeeded:Number(x.knownNeeded||0)+Number(x.tbdQty||0)*est,hoursFilled:Number(x.knownFilled||0)+Number(x.tbdFilledQty||0)*est})),volunteers:volunteers.results}}
